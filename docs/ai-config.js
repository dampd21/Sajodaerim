const AI_CONFIG = {
  AI_MODE: "proxy",              // "proxy" | "direct" | "disabled"
  GEMINI_PROXY_URL: "",          // 예: https://YOUR-WORKER.workers.dev/gemini
  GEMINI_API_KEY: "",            // direct 모드에서만 사용 (권장하지 않음)
  GEMINI_API_URL: "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent"
};
